#!/bin/bash
ME=${BASH_SOURCE[0]}
DIR=$(dirname $ME)

if [ "$(command -v pmset 2>/dev/null | wc -l | xargs)" = "1" ]; then
  ${DIR}/battery-pmset
elif [ "$(command -v acpi 2>/dev/null | wc -l | xargs)" = "1" ]; then
  ${DIR}/battery-acpi
else
  echo "(:X)"
fi
