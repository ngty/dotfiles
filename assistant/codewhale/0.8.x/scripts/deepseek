#!/usr/bin/env bash
# Launch CodeWhale with DeepSeek as the active provider.
# Switch inside the session with: /provider openai
SCRIPT_DIR="$(dirname "$(readlink -f "$0")")"
PROJECT_ROOT="$(dirname "$SCRIPT_DIR")"
export CODEWHALE_PROVIDER=deepseek
exec "$PROJECT_ROOT/bin/run.sh" "$@"
